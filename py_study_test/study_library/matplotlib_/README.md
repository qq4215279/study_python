# Matplotlib 学习指南

## 项目概述

这是一个完整的 Matplotlib 学习项目，包含了从基础到高级的所有重要概念和实用示例。

## 🚀 新增特性

✅ **完善的中文支持** - 所有文件均已配置UTF-8编码和中文字体支持
✅ **中文显示测试** - 包含专门的中文显示测试文件
✅ **多字体备选方案** - 支持多种中文字体以确保兼容性

## 文件说明

### 1. `matplotlib_introduction.py` - 基础入门
- **内容**：Matplotlib 核心概念和基础 API 详解
- **涵盖主题**：
  - Figure、Axes、Axis 等核心概念
  - 两种绘图方式对比（pyplot vs 面向对象）
  - 基础图表类型：线条图、散点图、柱状图、饼图、直方图等
  - 高级图表：双Y轴、极坐标、3D图
  - 图表自定义：样式、颜色、字体、注释等

### 2. `chart_types_detailed.py` - 图表类型详解
- **内容**：详细的图表类型和进阶技巧
- **涵盖主题**：
  - 时间序列图表
  - 分布图表（直方图、CDF、QQ图）
  - 对比图表（分组柱状图、雷达图、瀑布图）
  - 交互式元素
  - 动画效果
  - 样式定制

### 4. `chinese_test.py` - 中文显示测试
- **内容**：专门用于测试中文显示功能
- **功能**：
  - 基础中文标签测试
  - 特殊中文字符兼容性测试
  - 帮助诊断中文显示问题
- **内容**：可重用的图表模板和最佳实践
- **涵盖主题**：
  - 常用图表模板类
  - 数据可视化工具函数
  - 最佳实践演示
  - 专业色彩搭配
  - 仪表板布局示例

## 核心概念

### 1. 基本架构
```
Figure (整个图表窗口)
└── Axes (绘图区域)
    ├── X-axis (X坐标轴)
    ├── Y-axis (Y坐标轴)
    └── Artists (所有可见元素)
```

### 2. 两种绘图方式

**Pyplot 方式**（简单快速）
```python
import matplotlib.pyplot as plt
plt.plot(x, y)
plt.show()
```

**面向对象方式**（推荐，更灵活）
```python
fig, ax = plt.subplots()
ax.plot(x, y)
plt.show()
```

### 3. 常用图表类型

| 图表类型 | 用途 | 关键函数 |
|---------|------|----------|
| 线条图 | 显示趋势 | `plot()` |
| 散点图 | 显示关系 | `scatter()` |
| 柱状图 | 比较类别 | `bar()`, `barh()` |
| 饼图 | 显示比例 | `pie()` |
| 直方图 | 显示分布 | `hist()` |
| 箱线图 | 显示统计 | `boxplot()` |

## 中文支持说明

### 已配置的中文支持
所有文件都已添加以下配置：

```python
# -*- coding: utf-8 -*-  # 文件编码声明

# 设置中文字体支持
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei', 'Arial Unicode MS', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False
```

### 字体优先级
1. **SimHei** (黑体) - Windows系统默认中文字体
2. **Microsoft YaHei** (微软雅黑) - Windows现代界面字体
3. **Arial Unicode MS** - 跨平台Unicode字体
4. **DejaVu Sans** - Linux系统常用字体

### 测试中文显示
```bash
# 运行中文显示测试
python chinese_test.py
```

### 常见中文显示问题解决方案

1. **仍然显示方块或乱码**：
   ```python
   # 尝试其他字体
   plt.rcParams['font.sans-serif'] = ['FangSong', 'KaiTi']
   ```

2. **负号显示异常**：
   ```python
   plt.rcParams['axes.unicode_minus'] = False  # 已在配置中包含
   ```

3. **Linux系统字体安装**：
   ```bash
   # Ubuntu/Debian
   sudo apt-get install fonts-wqy-zenhei
   
   # CentOS/RHEL
   sudo yum install wqy-zenhei-fonts
   ```

### 运行示例

```bash
# 运行基础入门示例
python matplotlib_introduction.py

# 运行图表类型详解
python chart_types_detailed.py

# 运行实用工具示例
python visualization_utils.py
```

### 基础用法示例

```python
import matplotlib.pyplot as plt
import numpy as np

# 创建数据
x = np.linspace(0, 10, 100)
y = np.sin(x)

# 简单绘图
plt.figure(figsize=(10, 6))
plt.plot(x, y, 'b-', linewidth=2, label='sin(x)')
plt.xlabel('X轴')
plt.ylabel('Y轴')
plt.title('正弦函数图')
plt.legend()
plt.grid(True)
plt.show()
```

## 最佳实践

### 1. 图表设计原则
- ✅ 保持简洁清晰
- ✅ 使用合适的颜色对比
- ✅ 添加必要的标签和标题
- ✅ 合理使用网格线
- ❌ 避免过多装饰元素
- ❌ 不要使用难以区分的颜色

### 2. 颜色选择建议
```python
# 推荐的安全色系
colors = ['#1f77b4', '#ff7f0e', '#2ca02c', '#d62728', '#9467bd']

# 使用colormap
import matplotlib.cm as cm
colors = cm.Set1(np.linspace(0, 1, n_categories))
```

### 3. 字体和中文支持
```python
plt.rcParams['font.sans-serif'] = ['SimHei', 'Arial Unicode MS']
plt.rcParams['axes.unicode_minus'] = False
```

## 高级技巧

### 1. 子图布局
```python
# GridSpec 布局
gs = fig.add_gridspec(3, 3)
ax1 = fig.add_subplot(gs[0, :])  # 占据第一行全部
ax2 = fig.add_subplot(gs[1:, :2])  # 占据下面两行的前两列
```

### 2. 双Y轴
```python
ax1 = plt.gca()
ax2 = ax1.twinx()
ax1.plot(x, y1, 'b-')
ax2.plot(x, y2, 'r-')
```

### 3. 交互式功能
```python
# 启用拾取功能
line, = ax.plot(x, y, picker=True)
```

## 常见问题解决

### 1. 中文显示乱码
```python
# 解决方案
plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False
```

### 2. 图片保存模糊
```python
# 提高保存质量
plt.savefig('figure.png', dpi=300, bbox_inches='tight')
```

### 3. 布局重叠
```python
# 自动调整布局
plt.tight_layout()
# 或者手动调整
plt.subplots_adjust(left=0.1, bottom=0.1, right=0.9, top=0.9, wspace=0.4, hspace=0.4)
```

## 学习路径建议

1. **初学者**：先运行 `matplotlib_introduction.py` 理解基础概念
2. **进阶用户**：学习 `chart_types_detailed.py` 掌握各种图表类型
3. **专业用户**：使用 `visualization_utils.py` 中的模板和工具提高效率

## 参考资源

- [官方文档](https://matplotlib.org/stable/index.html)
- [Gallery示例](https://matplotlib.org/stable/gallery/index.html)
- [教程](https://matplotlib.org/stable/tutorials/index.html)

## 项目特点

✅ **完整覆盖**：从基础到高级的全面内容  
✅ **实用性强**：提供可重用的模板和工具  
✅ **中文友好**：完善的中文支持和注释  
✅ **最佳实践**：包含大量设计建议和技巧  
✅ **模块化设计**：代码结构清晰，易于学习和扩展  

---
*本项目旨在帮助您系统掌握 Matplotlib 数据可视化技能*